name: Релиз

on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      version:
        description: 'Версия, например 1.1.0'
        required: true

jobs:
  release:
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Поставить .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Определить версию
        id: ver
        shell: pwsh
        run: |
          $v = if ($env:GITHUB_REF -like 'refs/tags/v*') {
            $env:GITHUB_REF -replace '^refs/tags/v',''
          } else {
            '${{ github.event.inputs.version }}'
          }
          "version=$v" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
          "Версия: $v"

      - name: Обычная сборка
        run: dotnet publish -c Release -r win-x64 --self-contained false -o publish/setup -p:Version=${{ steps.ver.outputs.version }}

      - name: Портативная сборка
        run: >
          dotnet publish -c Release -r win-x64 --self-contained false
          -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true
          -p:Version=${{ steps.ver.outputs.version }}
          -o publish/portable

      - name: Собрать установщик
        shell: pwsh
        run: |
          # Inno Setup предустановлен на образе windows-latest
          $iscc = "${env:ProgramFiles(x86)}\Inno Setup 6\ISCC.exe"
          if (-not (Test-Path $iscc)) {
            choco install innosetup --no-progress -y
            $iscc = "${env:ProgramFiles(x86)}\Inno Setup 6\ISCC.exe"
          }
          & $iscc /DMyAppVersion=${{ steps.ver.outputs.version }} JL-Monitor-Brightness.iss

      - name: Переименовать портативную
        shell: pwsh
        run: |
          $v = '${{ steps.ver.outputs.version }}'
          Move-Item publish/portable/JL-Monitor-Brightness.exe "publish/JL_MonitorBrightness_${v}_Portable.exe"

      # Описание релиза берётся из CHANGELOG, а не генерируется из коммитов:
      # его же показывает окно обновления внутри программы, и человеку нужен
      # человеческий текст, а не список сообщений git.
      - name: Достать описание из истории версий
        shell: pwsh
        run: |
          $v = '${{ steps.ver.outputs.version }}'
          $md = Get-Content CHANGELOG.md -Raw
          $m = [regex]::Match($md, "(?ms)^## \[$([regex]::Escape($v))\][^\n]*\n(.*?)(?=^## \[|\z)")
          if ($m.Success) {
            $m.Groups[1].Value.Trim() | Out-File -FilePath release-notes.md -Encoding utf8
          } else {
            "Версия $v" | Out-File -FilePath release-notes.md -Encoding utf8
            Write-Host "В CHANGELOG нет раздела для $v — описание будет пустым"
          }
          Get-Content release-notes.md

      - name: Выложить релиз
        uses: softprops/action-gh-release@v2
        with:
          name: v${{ steps.ver.outputs.version }}
          tag_name: v${{ steps.ver.outputs.version }}
          body_path: release-notes.md
          files: |
            Output/*.exe
            publish/*.exe
