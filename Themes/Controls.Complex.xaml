<!--
  Выпадающий список, вкладки-сегменты и полоса прокрутки.
  Самые громоздкие шаблоны: ошибка здесь ломает не вид, а поведение.
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <!-- ── ComboBox ────────────────────────────────────────────────────────── -->

  <ControlTemplate x:Key="ComboToggleTemplate" TargetType="ToggleButton">
    <Border x:Name="Bd" CornerRadius="10" Height="32"
            Background="{DynamicResource Card}"
            BorderBrush="{DynamicResource Hairline}" BorderThickness="1">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <ContentPresenter Grid.Column="0" Margin="12,0,6,0" VerticalAlignment="Center"/>
        <Path Grid.Column="1" Margin="0,0,12,0" VerticalAlignment="Center"
              Data="M 0,0 L 4,4 L 8,0" Stroke="{DynamicResource Ink4}"
              StrokeThickness="1.5" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
      </Grid>
    </Border>
    <ControlTemplate.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource HairlineStrong}"/>
      </Trigger>
      <Trigger Property="IsChecked" Value="True">
        <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
      </Trigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>

  <Style x:Key="HaloComboItem" TargetType="ComboBoxItem">
    <Setter Property="Padding" Value="12,0"/>
    <Setter Property="Height" Value="30"/>
    <Setter Property="FontSize" Value="13"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ComboBoxItem">
          <Border x:Name="Bd" CornerRadius="7" Background="Transparent" Margin="4,1"
                  Padding="{TemplateBinding Padding}">
            <ContentPresenter VerticalAlignment="Center"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
              <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Hover}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
              <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Hairline}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="HaloCombo" TargetType="ComboBox">
    <Setter Property="FontSize" Value="13"/>
    <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
    <Setter Property="ItemContainerStyle" Value="{StaticResource HaloComboItem}"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ComboBox">
          <Grid>
            <ToggleButton x:Name="ToggleButton" Focusable="False" ClickMode="Press"
                          Template="{StaticResource ComboToggleTemplate}"
                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>
            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                              Content="{TemplateBinding SelectionBoxItem}"
                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                              Margin="12,0,28,0" VerticalAlignment="Center"/>

            <Popup x:Name="PART_Popup" AllowsTransparency="True"
                   IsOpen="{TemplateBinding IsDropDownOpen}"
                   Placement="Bottom" VerticalOffset="4" PopupAnimation="Fade" Focusable="False">
              <Border Background="{DynamicResource Card}" CornerRadius="12"
                      BorderBrush="{DynamicResource Hairline}" BorderThickness="1"
                      Padding="0,4" Margin="0,0,8,8"
                      MinWidth="{TemplateBinding ActualWidth}"
                      MaxHeight="{TemplateBinding MaxDropDownHeight}"
                      Effect="{StaticResource Shadow2}">
                <ScrollViewer>
                  <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                </ScrollViewer>
              </Border>
            </Popup>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ── Вкладки-сегменты вместо TabControl ──────────────────────────────── -->

  <Style x:Key="SegmentTabs" TargetType="TabControl">
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="TabControl">
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Высота полосы и вкладок связаны: вкладка занимает её целиком минус
                 padding, иначе кнопки болтаются внутри и выглядят узкими полосками. -->
            <Border Grid.Row="0" CornerRadius="18" Height="40" Padding="4"
                    Background="{DynamicResource Hairline}" HorizontalAlignment="Stretch">
              <TabPanel IsItemsHost="True" Panel.ZIndex="1"/>
            </Border>

            <!-- Смена вкладки: содержимое всплывает снизу за 0.2 с. Без этого
                 переключение выглядит как мгновенная подмена картинки. -->
            <ContentPresenter x:Name="Content" Grid.Row="1" Margin="0,18,0,0"
                              ContentSource="SelectedContent"
                              RenderTransformOrigin="0.5,0.5">
              <ContentPresenter.RenderTransform>
                <TranslateTransform x:Name="ContentSlide"/>
              </ContentPresenter.RenderTransform>
            </ContentPresenter>
          </Grid>

          <!--
            ⛔ Здесь НЕЛЬЗЯ вешать EventTrigger на Selector.SelectionChanged.
            Это маршрутизируемое событие: его поднимает любой ComboBox внутри вкладки,
            оно всплывает до TabControl, триггер срабатывает на чужое событие и падает
            с «Не удаётся найти имя Content» — потому что ищет цель не в той области имён.
            Проверено на живой Windows 25.08.2026: приложение не запускалось вовсе.

            Анимация смены вкладки живёт в TabItem (см. SegmentTabItem ниже) — там она
            привязана к состоянию самого элемента и чужих событий не ловит.
          -->
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="SegmentTabItem" TargetType="TabItem">
    <Setter Property="VerticalAlignment" Value="Stretch"/>
    <Setter Property="Height" Value="32"/>
    <Setter Property="FontSize" Value="13"/>
    <Setter Property="FontWeight" Value="Medium"/>
    <Setter Property="Foreground" Value="{DynamicResource Ink3}"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="TabItem">
          <Border x:Name="Bd" CornerRadius="14" Background="Transparent"
                  Padding="16,0" VerticalAlignment="Stretch">
            <ContentPresenter ContentSource="Header" VerticalAlignment="Center"
                              HorizontalAlignment="Center"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsSelected" Value="True">
              <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Card}"/>
              <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
              <Setter Property="FontWeight" Value="SemiBold"/>
              <Setter TargetName="Bd" Property="Effect" Value="{StaticResource Shadow1}"/>
              <Trigger.EnterActions>
                <BeginStoryboard>
                  <Storyboard>
                    <DoubleAnimation Storyboard.TargetName="Bd" Storyboard.TargetProperty="Opacity"
                                     From="0.5" To="1" Duration="0:0:0.16"/>
                  </Storyboard>
                </BeginStoryboard>
              </Trigger.EnterActions>
            </Trigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="IsSelected" Value="False"/>
                <Condition Property="IsMouseOver" Value="True"/>
              </MultiTrigger.Conditions>
              <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
            </MultiTrigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ── Тонкая полоса прокрутки ─────────────────────────────────────────── -->

  <Style x:Key="ThinScrollBar" TargetType="ScrollBar">
    <Setter Property="Width" Value="8"/>
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ScrollBar">
          <Track x:Name="PART_Track" IsDirectionReversed="True">
            <Track.Thumb>
              <Thumb>
                <Thumb.Template>
                  <ControlTemplate TargetType="Thumb">
                    <Border CornerRadius="3" Width="6" Background="{DynamicResource HairlineStrong}"/>
                  </ControlTemplate>
                </Thumb.Template>
              </Thumb>
            </Track.Thumb>
            <Track.IncreaseRepeatButton>
              <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="False"/>
            </Track.IncreaseRepeatButton>
            <Track.DecreaseRepeatButton>
              <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="False"/>
            </Track.DecreaseRepeatButton>
          </Track>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

</ResourceDictionary>
